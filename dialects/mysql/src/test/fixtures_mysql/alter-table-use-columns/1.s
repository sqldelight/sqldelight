CREATE TABLE test(
  id VARCHAR(8)
);

ALTER TABLE test
  ADD COLUMN id2 VARCHAR(32) NULL DEFAULT NULL,
  ADD COLUMN id3 VARCHAR(32) NULL DEFAULT NULL,
  ADD UNIQUE KEY unq_id3 (id3);

CREATE TABLE test2(
  id VARCHAR(8)
);

ALTER TABLE test2
  ADD COLUMN id2 VARCHAR(32) NULL DEFAULT NULL,
-- error[col 26]: No column found with name id3
  ADD UNIQUE KEY unq_id3 (id3),
  ADD COLUMN id3 VARCHAR(32) NULL DEFAULT NULL;