apply plugin: 'org.jetbrains.kotlin.platform.jvm'

archivesBaseName = 'sqldelight-runtime'

sourceSets {
  main.kotlin.srcDirs += '../src/jvmMain/kotlin'
  test.kotlin.srcDirs += '../src/jvmTest/kotlin'
}

dependencies {
  expectedBy project(':runtime:sqldelight-runtime:common')
  implementation deps.kotlin.stdlibJdk

  testImplementation deps.kotlin.testJunit
  testImplementation project(':runtime:sqlite-driver')
}

apply from: "$rootDir/gradle/gradle-mvn-push.gradle"
