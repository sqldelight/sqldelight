CREATE TABLE MyJson(
    data TEXT NOT NULL
);

insert:
INSERT INTO MyJson (data) VALUES (?);

selectAlpha:
SELECT data->'alpha' AS a, data->>'alpha' AS aa
FROM MyJson;

selectBindArgs:
SELECT data->? AS a, data->>? AS aa
FROM MyJson;
