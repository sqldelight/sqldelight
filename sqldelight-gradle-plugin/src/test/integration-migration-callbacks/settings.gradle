pluginManagement {
    includeBuild("../build-logic-tests")
}

plugins {
    id("sqldelightTests")
}

rootProject.name = 'integration'

buildCache {
  local {
    directory = new File(rootDir, 'build-cache')
  }
}
