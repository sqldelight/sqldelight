plugins {
  alias(libs.plugins.kotlin.jvm)
  alias(sqldelightDeps.plugins.sqldelight)
}

sqldelight {
  databases {
    Database {
      packageName = "com.example"
      dialect(sqldelightDeps.dialects.mysql)
    }
  }
}
