pluginManagement {
    includeBuild("../build-logic-tests")
}

plugins {
    id("sqldelightTests")
}

rootProject.name = 'cache-variant'

buildCache {
  local {
    directory = new File(rootDir, 'build-cache')
  }
}
