CREATE TABLE hockeyPlayer (
  id INTEGER NOT NULL PRIMARY KEY,
  first_name TEXT NOT NULL,
  last_name TEXT AS String NOT NULL
);

CREATE VIEW hockeyPlayerView AS
SELECT *
FROM hockeyPlayer;

SELECT * FROM hockeyPlayerView;