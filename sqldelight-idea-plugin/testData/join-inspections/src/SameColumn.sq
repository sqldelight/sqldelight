CREATE TABLE test(
  value TEXT NOT NULL
);

CREATE TABLE test2(
  value2 TEXT NOT NULL
);

someSelect:
SELECT *
FROM test
JOIN test2 ON (value = value);

properSelect:
SELECT *
FROM test
JOIN test2 ON (value = value2);